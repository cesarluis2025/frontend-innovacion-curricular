@page
@model FrontendInnovacionCurricular.Pages.CarInnovacion.IndexModel
@{ ViewData["Title"] = "Característica de innovación"; }
<h1>Característica de innovación</h1>
<a class="boton" href="/CarInnovacion/Crear">+ Nuevo registro</a>
<table>
    <thead><tr><th>ID</th><th>Nombre</th><th>Descripción</th><th>Tipo</th><th></th></tr></thead>
    <tbody>
        @foreach (var item in Model.Registros)
        {
            <tr>
                <td>@item.Id</td><td>@item.Nombre</td><td>@item.Descripcion</td><td>@item.Tipo</td>
                <td class="acciones">
                    <a href="/CarInnovacion/Editar/@item.Id">Editar</a>
                    <form method="post" asp-page-handler="Eliminar" asp-route-id="@item.Id" style="display:inline" onsubmit="return confirm('¿Eliminar este registro?');">
                        <button type="submit">Eliminar</button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>
