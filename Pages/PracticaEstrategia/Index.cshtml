@page
@model FrontendInnovacionCurricular.Pages.PracticaEstrategia.IndexModel
@{ ViewData["Title"] = "Práctica / estrategia"; }
<h1>Práctica / estrategia</h1>
<a class="boton boton-primario" href="/PracticaEstrategia/Crear">+ Nuevo registro</a>
<table>
    <thead><tr><th>ID</th><th>Tipo</th><th>Nombre</th><th>Descripción</th><th></th></tr></thead>
    <tbody>
        @foreach (var item in Model.Registros)
        {
            <tr>
                <td>@item.Id</td><td>@item.Tipo</td><td>@item.Nombre</td><td>@item.Descripcion</td>
                <td class="acciones">
                    <a href="/PracticaEstrategia/Editar/@item.Id">Editar</a>
                    <form method="post" asp-page-handler="Eliminar" asp-route-id="@item.Id" style="display:inline" onsubmit="return confirm('¿Eliminar este registro?');">
                        <button type="submit">Eliminar</button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>
