@page
@model FrontendInnovacionCurricular.Pages.AspectoNormativo.IndexModel
@{ ViewData["Title"] = "Aspecto normativo"; }
<h1>Aspecto normativo</h1>
<a class="boton boton-primario" href="/AspectoNormativo/Crear">+ Nuevo registro</a>
<table>
    <thead><tr><th>ID</th><th>Tipo</th><th>Descripción</th><th>Fuente</th><th></th></tr></thead>
    <tbody>
        @foreach (var item in Model.Registros)
        {
            <tr>
                <td>@item.Id</td><td>@item.Tipo</td><td>@item.Descripcion</td><td>@item.Fuente</td>
                <td class="acciones">
                    <a href="/AspectoNormativo/Editar/@item.Id">Editar</a>
                    <form method="post" asp-page-handler="Eliminar" asp-route-id="@item.Id" style="display:inline" onsubmit="return confirm('¿Eliminar este registro?');">
                        <button type="submit">Eliminar</button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>
